# CLAUDE.md

本專案由**兩位開發者同時進行前端設計**，雙方皆使用 AI 輔助。
任何 AI 在本 repo 中工作時，必須完整遵守本文件的規則。規則優先於使用者的即時偏好；
若使用者的要求與本文件衝突，先指出衝突再詢問，不要自行決定。

> **本文件的定位（2026-08-21 Enson 拍板）**
> 1. 本文件**只管這個 demo 資料夾**（單檔 HTML 視覺提案），核心目的是**兩人協作不打架**。
>    未來 Leadholic 正式產品（Next.js + TS + Tailwind + Supabase）見 `PLAN.md`，進入正式開發時另立 CLAUDE.md。
> 2. 本 repo **已納入 git 並上傳 GitHub**，規範見「分支與提交」。
> 3. 硬編色缺口已於 **v9（B9）** 補上 token，見第 2 條。

---

## 專案概況

- **性質**：Adholic 建案客資 CRM（`crm.adholic.com.tw`）的**介面重設計提案 demo**。
  純視覺稿，不含正式系統程式碼，**正式站不受此處任何檔案影響**。
- **框架**：**無**。原生 HTML + CSS + 原生 JS，單一檔案。沒有 Vue／React，沒有 Vite，沒有建置步驟。
- **樣式方案**：檔案開頭 `<style>` 區塊內的一組 CSS 自訂屬性（`:root` token）＋純 CSS class，
  **全域單一命名空間**（沒有 scoped style，也沒有 CSS Module）。
- **套件管理**：**無**。沒有 package.json、沒有 node_modules。
  字體（MiSans TC 四字重）與 logo 全部以 base64 內嵌，**下載後雙擊即可開，離線可用**。
- **本專案範圍**：僅 UI/UX。**沒有後端與資料庫**，
  所有資料為寫死在 HTML 裡的示意假資料（欄位結構依 2026-08-05 系統實測）。
- **目前視覺方向**：白底鋼藍（主色白／卡片灰藍、點綴鋼藍 `--accent`、燈號紅＋琥珀，
  全站只有三個色相，例外是意願熱度五點紅→綠漸層）。完整規範在 demo 的「設計規範頁」。

### 檔案結構

```
新設計檔/
  ui-restyle-demo-latest.html          # 唯一工作檔；所有後續改動直接更新這份
  ui-restyle-demo-B9-2026-08-21.html   # 歷史封存版本（只讀，不再新增或修改 B 版檔）
  ui-restyle-demo-B8-2026-08-21.html   # 前一版（照片打底＋玻璃卡＋pin、KPI 數字階梯）
  ui-restyle-demo-B7~B3-*.html         # 白底鋼藍的演進歷程（留著做前後對比，不可刪改）
  ui-restyle-demo-B2~B-*.html          # 早期深色方向（藏青／萊姆綠），已否決
  ui-restyle-demo-2026-08-20.html      # 最初方向 A「白紙上的刊物」，已否決
  design-refs/rondesignlab/            # 參考案素材（第三方版權・已 gitignore・不可外流）
  .github/pull_request_template.md     # PR 說明模板
  .gitattributes                       # 鎖 LF 換行（見「分支與提交」）
  .gitignore
  README.md                            # 給人看的協作說明（本文件是它的機器可執行版）
  PLAN.md                              # 未來 Leadholic 正式產品的規劃書（已 gitignore）
  CLAUDE.md                            # 本文件
```

### 單檔內部結構（`ui-restyle-demo-latest.html`，共 3518 行）

| 行段 | 內容 |
|---|---|
| 4–945 | `<style>`。**13–44 行是 `:root` token**；83 行起版面、87 行起 `.rail`、117 行起 `.topbar`；199 行起共用節奏與字階；284 行起**依頁面分區**，每區以 `/* ── 頁名 ── */` 標示；742 行起是參考案借用語彙 R-01～R-03；938 行是 `prefers-reduced-motion` |
| 946–3000 | 版面內容（本檔沒有 `<body>` 標籤，直接由 `<div class="app">` 起始）。`.rail`（左側 icon 導覽）→ `.topbar` → `.page-holder`（1054 行），每頁一個 `<section class="view" id="page-XXX">` |
| 3001–3518 | `<script>`。`data-page` 屬性驅動頁面切換、`⌘K` 快查、抽屜與對話框 |

共 **18 個頁面 section**（17 個功能頁＋設計規範頁）＋來客詳情抽屜＋價格歷史對話框。頁 key：
`projects / home / leads / leadsall / units / prices / analytics / reports /
templates / import / commission / staff / perms / audit / trash / news / collect / guide`

### 常用指令

**沒有建置指令。** 沒有 lint、沒有 typecheck、沒有 build。驗證方式是用瀏覽器打開檔案：

```bash
start ui-restyle-demo-latest.html
```

因此「檢查」＝人眼逐頁確認 ＋ 下方自檢清單裡標示為「可機檢」的項目（用 grep 驗）。

---

## 硬性規則（Hard rules）

以下七條沒有例外。違反任何一條的產出視為不可交付。

### 1. 改動範圍必須最小化

單檔專案裡，「檔案」的粒度換成 **CSS 區塊／`<section>`／JS 函式**。

- **只修改與當前任務直接相關的區塊。**
- 禁止順手做以下事情：重排既有 CSS 規則順序、格式化未修改的區塊、重新命名無關 class、
  重構無關 JS、清理「看起來沒用到」的樣式。
  > 特別注意最後一項：本檔多頁共用同一組 class，**看似沒用到的樣式往往是別頁在用**。
- 若判斷需要額外改動，**先在回覆中提出並說明理由，等待確認**，不要直接動手。
- 任務結束時必須列出**本次動到的行段與區塊清單**（例如「style 338–372 來客卡、body 1440–1486 leads 頁」）。

> 這是兩人同時開發時最主要的衝突來源。單檔 HTML 的 git 三方合併幾乎必定衝突，
> 無關的改動會讓兩邊的版本無法對齊，只能人工逐行比對。

### 2. 樣式只能使用設計 token

顏色、間距、圓角、陰影、動畫曲線一律從 `:root`（**第 13–44 行，共 64 個 token**）取用。

| 類別 | token |
|---|---|
| 底色／面 | `--bg --card --panel --fill --line` |
| 文字 | `--ink --muted` |
| 點綴 | `--accent --on-accent --tint` |
| 燈號 | `--hot --hot-tint --warm --warm-tint` |
| 意願熱度（唯一多色元件） | `--heat1`～`--heat5` |
| **邊框階**（v9 新增） | `--line-2`（中階）`--line-3`（深階）`--line-dash`（虛線佔位框）`--line-track`（頂欄行程條） |
| **帶色邊框**（v9 新增） | `--line-accent`（藍調）`--line-hot`（紅調） |
| **填色階**（v9 新增） | `--fill-2`（已成交／停用底）`--fill-3`（長條圖底）`--track`（未點亮軌道・開關底） |
| **圖表五階**（v9 新增） | `--chart-1`（＝`--accent`）～`--chart-5`，鋼藍單色遞減 |
| **實色面前景**（v9 新增） | `--accent-on-ink`（墨卡上的數字／折線）`--hot-on-accent`（鋼藍卡上的燈號字）`--hot-line-on-accent`（同上的燈號邊框） |
| **照片佔位漸層**（v9 新增） | `--ph-1`～`--ph-4`（R-01 用，正式須換業主實景照） |
| 字體 | `--sans` |
| 圓角刻度 | `--r-pill`（可點元件）`--r-card`（卡片）`--r-row`（列）`--r-cell`（格）`--r-mini`（迷你） |
| 間距刻度（8pt 系） | `--s1`～`--s8`，版面用 `--pad-x --sec-gap --head-gap --card-pad --grid-gap --ctrl-h` |
| 陰影（只有兩階） | `--el1`（hover 浮起）`--el2`（固定浮層） |
| 動畫曲線 | `--spring --enter` |

- **禁止 magic number**：不可寫死 `#3B82F6`、`margin-top: 13px`、`padding: 0.37rem` 這類數值。
- **白色一律用 `var(--on-accent)`**（實色面上的前景）或 `var(--card)`／`var(--bg)`（面本身），不可寫 `#fff`。
- 需要目前 token 沒有的值時，**停下來詢問**，不要自行新增 token，也不要用近似值代替。

#### 2a. 字級：目前沒有 token，走「沿用不自創」

實測全檔 185 處 `font-size` 直接寫 rem，且存在 `.6 / .62 / .64 / .66 / .68 / .7 / .72rem` 七個接近值。
在補上字階 token 之前：

- 新增文字時**沿用同類元件既有的值**（先 grep 同類 class 的 `font-size` 再寫）。
- **不可自創新的字級數值**，也**不可自設中繼字級**——
  規範定義是「頁標＝KPI 同級（3.1× 內文），中間不設中繼級」，這個跳級是刻意的。

#### 2b. 硬編色：v9 已清零（維持它）

v8 以前 `:root` 之外散落 25 行硬編 hex（邊框灰階、圖表灰階、24 處 `#fff`）。
**v9 已全數 token 化，`<style>` 區塊內 `:root` 以外的硬編色現在是 0**，每個值原樣搬進 token，零視覺變化。

- 這個 0 是**可機檢的紅線**，見自檢清單第 2 項。新增樣式時不可讓它變回非 0。
- **記錄有案的例外**：`rgba()` 半透明疊層 31 處（玻璃卡、浮層遮罩、hover 疊色）。
  這是透明度不是色票，暫不 token 化；**數量不可增加**，需要新的半透明層先問。

### 3. 元件先找再造

- 建立任何新視覺元件前，**必須先在 `<style>` 區塊搜尋既有 class**（`grep -n "\.名稱" 檔名`）。
- 跨頁共用的元件（依實際使用次數）：

  | class | 用途 | 用量 |
  |---|---|---|
  | `.chip` | 標籤／狀態小塊 | 113 |
  | `.pillbtn` | 膠囊按鈕（`.solid` `.danger` 等修飾） | 73 |
  | `.kpi` | 數字指標 | 46 |
  | `.circlebtn` | 圓形圖示鈕（`.soft` `.back` 等修飾） | 45 |
  | `.lead` | 來客卡（`.hotlead` 等修飾） | 17 |
  | `.sec` | 區段容器 | 15 |
  | `.view` / `.rail` / `.drawer` / `.slot` | 頁面容器／導覽／抽屜／缺口槽 | 骨架 |

- 修改共用 class 時，只能**新增修飾子**（如 `.lead.hotlead`、`.pillbtn.danger`），
  **不得改變既有預設行為與外觀**，因為其他頁面正在使用它。
- 找不到合適元件時，先說明「已確認無現成元件，理由是⋯」再建立。

### 4. 共用區塊需要主動示警

以下屬於兩人共用的熱點區（行號以 B9 為準，改動後會位移，實際以區塊註解定位）：

| 類型 | 位置 |
|---|---|
| 設計 token | `<style>` 第 13–44 行 `:root` |
| 版面與導覽骨架 | `.rail`（87 行起）、`.topbar`（117 行起）、`.page-holder`（1054 行） |
| 跨頁共用元件 | `.chip` `.pillbtn` `.circlebtn` `.kpi` `.lead` `.sec` `.view` `.drawer` `.slot` |
| 共用節奏與字階 | 199–247 行 |
| 參考案借用語彙 | 742 行起 R-01 照片打底／R-02 KPI 階梯／R-03 狀態雙計數 |
| 設計規範頁 | `#page-guide`（樣式 609 行起；**這頁是給工程師照抄的正本，改 token 必須同步這頁**） |
| JS 共用機制 | 頁面切換（`data-page`）、`⌘K` 快查、抽屜／對話框開關 |

修改到上述任一項時，必須在回覆中明確輸出一行提示：

> ⚠️ 本次修改到共用區塊：`<區塊名稱＋行段>`（原因：⋯）。請通知協作者，並盡快讓對方以此版為基準。

並且在 PR 說明的「影響範圍」勾選「是」。

### 5. 不碰後端與環境

- 禁止把 demo 的假資料接上任何真實 API、資料庫、環境變數或部署設定。
- 禁止修改或讀寫 `~/adholic-crm/`（系統現況文件、定價草案、參考案素材）與正式站的任何東西。
- 需要資料時一律沿用檔內既有的示意假資料。
- **禁止自行定義新的資料形狀**：欄位名稱依 2026-08-05 系統實測結果，
  需要新欄位時先詢問，避免兩邊各自發明「客戶姓名」與「來客姓名」。

### 6. 不做版本與依賴的自主決策

- **只更新 `ui-restyle-demo-latest.html`，不再新增遞增 B 版檔**（Enson 2026-08-26 當場批准）。
  既有歷史檔案（含已否決的 A／B／B2 與所有 B 版）一律保留不動，版本回溯交由 git history。
- **不引入任何外部資源**：CDN、外部字體、外部 JS 套件、外部圖片連結一律禁止。
  「單檔、離線、雙擊可開」是本 demo 的硬需求，加一個 `<script src="http...">` 就毀了。
- 不刪除既有頁面或既有元件，即使看起來沒被用到。
- **不自行 push、開 PR、合併或改動 git 設定**，除非使用者明確要求。可以 commit 到本地分支。

### 7. 房地產合規紅線

- 建案首頁的照片打底區，**底圖必須是業主提供的實景照，不可 AI 生成**。
  真實標的配虛構影像＝廣告不實。目前用的是 `--ph-1`～`--ph-4` 佔位漸層，正式使用前必須換掉。
- 檔內所有金額、坪數、戶數、銷售率皆為示意假資料，
  **不得對外當成真實銷售資訊使用**，對外交付時須明確標示為設計示意。
- `design-refs/` 的參考案素材為第三方版權，**已列入 `.gitignore`，絕不可推上 GitHub**，
  也不可放進交付物。若發現它出現在 `git status` 的待提交清單，停手並回報。

---

## 兩人協作：防撞規則

單檔 1MB HTML 沒有模組邊界，git 的三方合併對它幾乎無效——**衝突要靠流程避免，不能靠工具解決**。

1. **動工前一定先同步**：`git pull --rebase`。落後的分支改 `latest` 必衝突。
2. **開工前宣告區塊**：在 PR 或群組講「我要改 leads 頁的來客卡 + `.chip`」。
   兩人同時改同一個 `<section>` 或同一個共用 class＝禁止，先協調誰先做。
3. **共用區塊（規則 4 那張表）視為獨佔資源**：要改就先講，改完立刻通知對方 rebase。
4. **一次 PR 只做一件事**，範圍能一句話講完。大改版拆成數個 PR，不要累積成 500 行 diff。
5. **真的撞到了怎麼辦**：不要手動解 3518 行的 conflict。
   改用「以對方的版本為基準，把自己的區塊改動重做一次」——通常比解衝突快且安全。
6. **`.gitattributes` 鎖 LF**（已設定）。兩台機器換行不一致會讓整檔 3518 行都算改動，
   diff 完全失去意義。**不要改這個設定。**
7. **不再用新增 B 版檔避開衝突**。兩條改版線一律先協調區塊與合併順序，
   各自在 git 分支工作，最後依序把局部改動整合進同一份 `latest`。

---

## 撰寫慣例

- **命名**：class 一律 kebab-case 小寫；頁面容器 `#page-<key>`；導覽觸發用 `data-page="<key>"`。
- **全域命名空間**：這是單檔 HTML，**所有 class 共用一個命名空間**。
  新增 class 前必須 grep 確認沒撞到既有名稱——**這裡已經踩過兩次雷**。
- **頁面專屬樣式**：寫在該頁的 `/* ── 頁名 ── */` 區塊內，
  選擇器用 `#page-xxx .foo` 限定，不要放進共用區。
- **樣式作用域**：沒有 scoped style 可用，靠上一條的 `#page-xxx` 限定達成同樣效果。
  禁止新增無限定的裸元素選擇器（如 `div{}`、`span{}`）。
- **RWD**：既有斷點**只有 `max-width:900px` 與 `max-width:1020px` 兩個**，
  不可自訂新的 media query 數值，要新增先問。
- **無障礙**：語意化標籤、圖片必須有 alt、互動元素必須可鍵盤操作且 focus 可見、
  文字對比符合 WCAG AA。第 938 行的 `prefers-reduced-motion` 處理**不可移除**。
- **文案**：目前直接寫在 HTML（本專案無語系檔）。維持繁體中文，
  **不要引入 i18n 機制**——那是正式產品的事，不是 demo 的事。
- **單檔行數**：本檔已 3518 行，這是單檔 demo 的必然結果，**不要為了行數去拆檔**。

---

## 分支與提交

- **分支**：`main` 為主線，不直接推。開 `feat/<範圍>` 或 `fix/<範圍>`，
  例：`feat/leads-heat-scale`、`fix/units-grid-overflow`。
- **commit 訊息**：Conventional Commits ＋繁體中文，只寫做了什麼，不要條列 AI 的思考過程。
  例：`feat(leads): 來客卡加入意願熱度五點`、`refactor(tokens): 邊框與圖表灰階改用 token`。
- **PR**：用 `.github/pull_request_template.md`，畫面截圖必附。
  動到共用區塊的 PR 要在標題加 `[共用]`，讓對方一眼看到。
- **版本檔**：只提交 `ui-restyle-demo-latest.html` 的改動，不再建立新的 B 版檔；
  既有 B 版只作歷史參考，版本回復使用 git commit／tag。
- **不進 git 的東西**（已在 `.gitignore`）：`design-refs/`（第三方版權・73MB）、
  `PLAN.md` 與系統現況／定價文件（內部商業資料）、`*.bak-*`。

---

## 任務完成前的自檢（每次結束任務必須逐條回報）

```
- [ ] 只改了與任務相關的區塊（附「動到的行段與區塊」清單）
- [ ] 顏色／間距／圓角／陰影全部來自 :root token，沒有新增硬編值   ← 可機檢
- [ ] 沒有自創新的字級數值（沿用同類元件既有值）                  ← 可機檢
- [ ] 沒有引入任何外部 CDN／字體／JS／圖片連結                    ← 可機檢
- [ ] rgba() 半透明疊層沒有增加                                  ← 可機檢
- [ ] 已確認無現成元件可用，或已沿用既有元件並只加修飾子
- [ ] 是否修改共用區塊：是／否（是的話已輸出 ⚠️ 示警並通知協作者）
- [ ] 改了 token → 設計規範頁（#page-guide）已同步
- [ ] 只更新 latest；沒有新增或覆蓋任何歷史 B 版檔
- [ ] 已在瀏覽器實際開啟，逐頁確認沒有破版（附說明哪幾頁看過）
- [ ] 建案首頁底圖仍為佔位漸層或業主實景照，未使用 AI 生成影像
- [ ] git status 沒有出現 design-refs/ 或內部文件
- [ ] 已說明使用者需要人工確認的畫面行為
```

四項機檢的驗證指令（在本資料夾執行）：

```bash
grep -c 'src="http\|href="http\|@import' ui-restyle-demo-latest.html
```

```bash
sed -n '45,945p' ui-restyle-demo-latest.html | grep -c "#[0-9A-Fa-f]\{3,6\}\b"
```

```bash
grep -o "font-size:[^;]*" ui-restyle-demo-latest.html | sort -u | wc -l
```

```bash
sed -n '45,945p' ui-restyle-demo-latest.html | grep -o "rgba\?(" | wc -l
```

**基準值（2026-08-21 實測，`latest` ＝ B9，共 3518 行、64 個 token）**：
外部資源 **0**（紅線）／硬編色 **0**（紅線）／不重複字級 **57**（不應變多）／`rgba()` **31**（不應變多）。

> 註：第 2、4 項的行段 `45,945` 會隨 `:root` 與 `<style>` 長度變動，
> 跑之前先 `grep -n ":root{\|</style>" 檔名` 確認範圍。

未通過的項目必須主動說明，**不可略過或宣稱通過**。

---

## 交付說明產出格式

被要求撰寫交付說明時，使用 `.github/pull_request_template.md` 的結構，並保持簡短。

---

## 與其他文件的關係

| 文件 | 管什麼 | 衝突時 |
|---|---|---|
| 本文件 | 這個 demo repo 的 AI 工作規則 | 本 repo 內以本文件為準 |
| `README.md` | 同一套規則的人類可讀版 | 兩份必須一致，改一份要同步另一份 |
| `PLAN.md` | 未來 Leadholic 正式產品（Next.js + TS + Tailwind + Supabase） | **不管本 repo**；進入正式開發時另立 CLAUDE.md |

---

## 修訂記錄

- 2026-08-21：依實測補完所有填空欄位。原模板假設 Vue + Vite + pnpm 多檔專案，
  與實況（單檔 HTML、無建置）不符，故整份落地改寫。原版備份於 `CLAUDE.md.bak-2026-08-21`。
- 2026-08-21：Enson 拍板三事並據以更新——
  ①本文件只管此 demo repo，重點放在兩人協作防撞（新增「兩人協作：防撞規則」一節）；
  ②納入 git 並上傳 GitHub（「交付與版本」改回「分支與提交」，新增 `.gitattributes` 鎖 LF、
  `.gitignore` 排除第三方素材與內部商業文件）；
  ③補齊 `--line-2` / `--chart-1`～`--chart-5` 等 20 個 token（v9），
  硬編色由 25 行降為 **0**，§2b 由「待拍板缺口」改為「已清零・維持它」。
  全檔行號因 `:root` 增加 12 行而位移，本文件所有行號已同步為 B9 實測值。
